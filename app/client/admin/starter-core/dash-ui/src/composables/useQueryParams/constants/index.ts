import { toBoolean } from '../helpers/toBoolean';
import { toNumber } from '../helpers/toNumber';
import { toString } from '../helpers/toString';
import { toStringLiteral } from '../helpers/toStringLiteral';

export const TRANSFORMS = {
    toString,
    toNumber,
    toBoolean,
    toStringLiteral
};
